import axios from "axios";

const getPlacesData = async () => {
    try{
        const response = await axios.get(); // 'await' pauses operations until 'axios.get()' sends a get request to the URL and retrieves data such as status code and other information
    } catch (error){

    }
}